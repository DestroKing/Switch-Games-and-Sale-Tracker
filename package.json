{
  "name": "switch-tracker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "menu": "bun run src/menu.ts",
    "probe": "bun run src/index.ts probe",
    "collect": "bun run src/index.ts collect",
    "fx": "bun run src/index.ts fx",
    "dashboard": "bun run src/web/server.ts",
    "start": "bun run src/menu.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
